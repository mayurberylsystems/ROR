<%#= select_tag :sub_category_id, options_for_select(@sub_categories.map{|c| [c.name, c.id]}), class: 'input-group filtering-select col-sm-2 form-control', name: 'sub_category_id[]', required: true, { :onchange => remote_function( :url => brand_change_sub_category_path(index: index), :with=>"'sub_catg_id='+this.value+''" ) } %>
<%= select_tag :sub_category_id, options_for_select(@sub_categories.map{|c| [c.name, c.id]}), class: 'input-group filtering-select col-sm-2 form-control', id: "sub_category_id_#{index}", name: 'sub_category_id[]', prompt: "Select Sub Category", data: { remote: true, url: url_for(action: "change_sub_catg", index: index)} %>
