<%= form_for :user, url: "/users/#{@user.id}", method: :put do |f| %>
	
	<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</p>
	<p>
		<%= f.submit "Update" %>
	</p>
<% end %>